spring:
  datasource:
    url: jdbc:postgresql://localhost/bank
    username: postgres
    password: 123456
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate # validate | update | create | create-drop
